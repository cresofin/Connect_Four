// You can experiment here, it won’t be checked

fun main(args: Array<String>) {
    fun main() {
        val color = readln()
        val options = Regex("colou?rs?")
        println(options.matches(color))
    }
}
